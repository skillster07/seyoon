# Windows W4b-0 등록 소스 로컬 검증 보고서 — 2026-08-26

## 범위

Windows에 `System + CurrentUser`로 영구 등록한 VIVIDCAM 가상 카메라를 설치
프로세스와 분리된 Media Foundation consumer가 symbolic link로 다시 열고,
1920×1080 NV12 60p 이동 컬러바를 실제 Frame Server 경로에서 수신하는지 검증했습니다.

## 설치 상태

- COM 서버: `C:\Program Files\VIVIDCAM\VirtualCamera\vividcam_virtual_camera_source.dll`
- 등록 장치: `VIVIDCAM Virtual Camera`
- 등록 범위: `System + CurrentUser`
- 빌드/설치 DLL SHA-256:
  `39ED02C0C872D2C2EF23E8FE13697BB9165C085626F8039A9200B46AEFB48A43`
- 빌드/설치 diagnostics SHA-256:
  `4EC24E7ACCD1172B4F1055EF87A75B596EA7DB39DC164B51070F3448A9F79A6A`

## 실행 명령

```powershell
.\native\scripts\install-virtual-camera.ps1 `
    -BuildDirectory .\native\build -SkipBuild -AllUsers
.\native\build\Release\vividcam_diagnostics.exe --registered-source-test
```

## 결과

```text
[registered-source] samples=12 type=1920x1080 NV12 60/1p checksums=12
timestamps=5691373789350..5691375622687
delta_avg/min/max=166667/166667/166667 durations=166667..166667
empty_callbacks=1 flags=0x100 [valid]
```

| 항목 | 결과 | 근거 |
| --- | --- | --- |
| 별도 프로세스 재열기 | 통과 | persistent symbolic link로 `MFCreateDeviceSource` 성공 |
| 미디어 타입 | 통과 | 1920×1080 NV12 60/1p |
| 샘플 전달 | 통과 | 12개 수신 |
| 프레임 변화 | 통과 | 서로 다른 checksum 12개 |
| 논리적 60p cadence | 통과 | timestamp delta 166667 고정 |
| sample duration | 통과 | 166667 고정 |
| Source Reader 상태 처리 | 통과 | 정상 `STREAMTICK(0x100)` 1회 후 계속 수신 |
| 로드된 DLL 업데이트 | 통과 | 설치기가 `FrameServer`를 중지·복구하고 해시 일치 확인 |

## 남은 로컬 범위

- Windows 재부팅 후 동일 symbolic camera 유지와 재수신
- OBS, SOOP, TikTok LIVE Studio의 장치 인식과 영상 수신
- W4b-1 엔진 producer의 실제 합성 프레임 전달과 wall-clock 60p pacing
- 장치·앱·엔진 재시작, 분리·재연결 및 장시간 안정성

